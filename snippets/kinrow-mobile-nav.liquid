<nav class="kinrow-mobile-nav" aria-label="Mobile quick navigation">
  <a href="{{ routes.root_url }}"{% if request.page_type == 'index' %} aria-current="page"{% endif %}>Home</a>
  <a href="{{ routes.all_products_collection_url }}"{% if request.page_type == 'collection' %} aria-current="page"{% endif %}>Shop</a>
  <a href="{{ routes.search_url }}"{% if request.page_type == 'search' %} aria-current="page"{% endif %}>Search</a>
  <a href="{{ routes.cart_url }}"{% if request.page_type == 'cart' %} aria-current="page"{% endif %}>Cart{% if cart.item_count > 0 %} ({{ cart.item_count }}){% endif %}</a>
</nav>
